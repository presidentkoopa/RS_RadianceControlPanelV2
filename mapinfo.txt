GameInfo
{
	// Order MOSTLY does not matter any more, and that is the point.
	//
	// It used to: DarkDoomZ, Sector Sweep and setpieces each wrote sector
	// light and colour directly, so whoever ran last won and this list decided
	// what the player saw. They now declare into GITD_Composite instead --
	// tints multiply, light deltas add, and both commute.
	//
	// Two orderings still matter, and only two:
	//   GITD_Composite is LAST because it flushes. Everything that wants a
	//     say must have had its tick before the write happens.
	//   GITD_PresetCustomiser is before GITD_Handler because it loads the
	//     selected preset into the working set the lanes read that same tick.
	AddEventHandlers = "DarkDoomZ_Handler", "GITD_PresetCustomiser", "GITD_Handler", "GITD_FlashlightHandler", "GITD_NeonKillCounter", "GITD_ResetHandler", "GITD_SetpieceConsole", "GITD_AmbushControl", "GITD_ColorLaw", "GITD_NeonDrawSync", "GITD_Composite"
}
